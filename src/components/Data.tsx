import React from 'react';
import FormControl from "./Form";
import Weather from "./Weather";

const Data = () => {
    return (
        <div>
            <FormControl/>
            <Weather/>
        </div>
    );
};

export default Data;